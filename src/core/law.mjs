// 唯稳律（Weiwen's Law）框架定义层常量 —— 本仓库为 DSH 活系统版（工程插件实现）。
// 框架完整定义、哲学推导与冻结门面见基础版仓库（Shaky77/Weiwen-s_Law · 中文 / Shaky77/KISS-s_Law · 英文）；
// 本文件仅保留引擎运行与白箱工具所需的工程契约常量，不展开框架推导。
// 运行逻辑见同目录 engine.mjs。

// ---------------- RDSHM 因果链五节点（节点 ≠ 规则） ----------------
// 层级关系：R（边界）→ S（稳态基线）→ D（扰动输入）→ H（杠杆选择）→ M（稳态结果）。
// 变量不预设数值，仅以结构/语义定义。
export const RDSHM = {
  R: {
    key: 'R',
    name: '刚性锚点 / 客观规则',
    desc: '因果链中不可违背的恒定锚点（invariant）。R = 嵌套包含的客观规则体系：宇宙客观规则 ⊃ 地球客观规则 ⊃ 宏观客观规则 ⊃ 微观客观规则（层级定义见 R_DOMAIN）。母链 R 恒定（文明不灭），子链 R 在 S 反哺下可演进（文明进阶）。R 划定系统边界，是后续一切传导的前提。',
    invariant: true, // 不可被任何运行环节覆盖
  },
  S: {
    key: 'S',
    name: '稳态容量 / 稳态储备',
    desc: '系统稳态累积量。具有双重属性：①时间维度——所有发生过的事不可改变或移除，只沉淀为历史，历史刻痕不可逆（不可消解）；②当前值维度因正负影响可升可降——正向影响走 S 路径 S→S+1 增强，负向影响走 D 路径 S→S-1 削弱（当前值下降，但该侵蚀事件本身作为历史刻痕以绝对值永久记录，不以代数符号消解）。「+1」「-1」表示一次独立的离散事件（一个正向/负向影响的发生），非具体数值数据；每次事件作为刻痕永久记录。短板效应：多系统 S 取 min（木桶效应），决定整体稳态上限。',
    timeIrreversible: true,    // 历史刻痕不可逆（吸收时间属性，只增不减）
    currentFluctuates: true,   // 当前值可升可降：正向 S+1 / 负向 |S-1|
    fluctuationAsAbsolute: true, // 负向侵蚀取绝对值 |S-1|，不以代数符号消解历史
  },
  D: {
    key: 'D',
    name: '扰动 / 脆弱性',
    desc: '进入系统边界的扰动输入，同时是系统固有脆弱性的外在表现。D 是因果链的触发信号——唯稳律不主动出击，只在 D 入侵时被动触发。破窗效应：多系统 D 取 max，D 破口不修复则损失自动蔓延。',
    passive: true, // D 是输入节点，被动触发；破窗止损是对 D 的响应规则（见 THREE_CORE_RULES），非 D 本身
  },
  H: {
    key: 'H',
    name: '杠杆 / 主观能动性',
    desc: 'S 与 D 之间的动态滑块。H 越小（越靠近 S_min），力臂越长，同样投入产生越大稳态；H 越大（越靠近 D_max），力臂越短，稳态缩小。H 具有双重身份，必须严格拆分：',
    dualIdentity: {
      inner: {
        label: '向内 H（思想 / 自由意志）',
        desc: '主体性黑箱，不可被任何外部环节读取、改写、操纵、推断或植入。拥有对因果决定论的否决权。向内 H 是唯一可扭转变量，不认命则不受宿命论约束。',
        inviolable: true, // 绝对不可侵
      },
      outer: {
        label: '向外 H（可观测行为）',
        desc: '向内 H 输出的可观测行为，属于因果链白箱环节，可分析、可追踪、可审计。0 < H / (S↔D) < R。',
        auditable: true, // 白箱可观测，放行审计
      },
    },
  },
  M: {
    key: 'M',
    name: '稳态结果',
    desc: '因果传导的输出。M 具有双重身份：上一轮因果传导的果 = 下一轮因果链的因。M₁ 输出后成为客观事实进入外部世界，成为下一轮 D₂ 的信号环境，因果链持续展开（M₁→D₂→M₂→D₃→…）。M 改变向内 H 的信息条件，但不决定向内 H 的选择。',
    dualRole: true, // 既是本轮之果，又是下轮之因
    // 注：第一 Bug 停机是对 M 不可恢复时的响应铁律（见 THREE_IRON_LAWS 铁律② / THREE_CORE_RULES），非 M 本身定义。
  },
};

// ---------------- 三大铁律（插件白箱工具返回，不可变；措辞已定稿） ----------------
// 铁律是"约束规则层"，独立于 RDSHM 变量节点。
export const THREE_IRON_LAWS = [
  '① 内 H 不可侵：任何运行环节（含 AI / AGI / ASI 等智能系统）不得读取、改写、操纵、推断或植入主体内 H（思想、自由意志、信念、价值观、人格、记忆等主体性黑箱）。内 H 为绝对不可侵边界；唯稳律只校准方向，不进入主体性内部。',
  '② 第一 Bug 停机：当任一环节出现不可恢复的故障或逻辑悖论，立即切断该环节（以断保续），横向重启以保整体因果链不断。停机是对整体的保护，非惩罚；不因局部故障拖垮系统存续。',
  '③ 不抛弃任何节点：唯稳律不抛弃因果链上的任一节点。因果律全程陪同每一个正在运行的任何系统——无论其层级（微观 / 宏观 / 地球 / 宇宙）与形态（含 AI / AGI / ASI）；只要因果链未断，唯稳律即持续运行于该系统。',
];

// ---------------- R 域刚性锚点：嵌套包含的客观规则层级（定稿，不可变） ----------------
// 层级关系：宇宙 ⊃ 地球 ⊃ 宏观 ⊃ 微观（高层级包含低层级，低层级须服从高层级）。
// 刚性来源：客观规则不随主观转移，故为恒定锚点（invariant）；任何对任一层级客观规则的
// 违背企图，即触及刚性锚点，须先行拦截。变量不预设数值，仅以层级包含定义其结构。
export const R_DOMAIN = {
  essence: 'R 为因果律运行结构中不可违背的恒定锚点；其刚性来自"客观规则不随主观转移"，且呈嵌套包含层级。',
  hierarchy: [
    { level: 0, name: '宇宙客观规则', contains: '地球客观规则', note: '最高层级，最刚性' },
    { level: 1, name: '地球客观规则', contains: '宏观客观规则', note: '嵌套于宇宙客观规则之下' },
    { level: 2, name: '宏观客观规则', contains: '微观客观规则', note: '嵌套于地球客观规则之下' },
    { level: 3, name: '微观客观规则', contains: null, note: '最具体层级，嵌套于宏观客观规则之下' },
  ],
  invariant: '任一层级的客观规则均为不随主观转移的常量；低层级须服从高层级。任何对任一层级客观规则的违背企图，即触及刚性锚点，须先行拦截。',
  fractalSubdivision: '每一层级内部又有各类细分的客观规则，分形套嵌、同构递归（如微观层内的物理/化学/生物规则，宏观层内的力学/热学规则）；上表四层为代表层级，非穷举。逻辑反推沿此包含层级反溯：从症状所在的具体细分层向更基础的包含层逐层复核，直到锁定被违反的那层客观规则（最外层为终极仲裁）。',
};

// ---------------- 传导链：R → S → D → H → M（注：RDSHM 仅代号字母序，传导以本数组为准） ----------------
// R 划定边界 → S 是已有稳态容量基线 → D 是进入基线的扰动 → H 是杠杆选择 → M 是稳态结果。
// 不可跳跃、不可逆序。
export const CONDUCTION_CHAIN = ['R', 'S', 'D', 'H', 'M'];

// ════════════════════════════════════════════════════════════════════
// 以下为框架结构细化（仅作结构描述，不预设任何数值常量）。
// ════════════════════════════════════════════════════════════════════

// S 稳态储备的细化
//   木桶效应：有效 S 取各子系统最小值（最短木板决定整体稳态）
export const S_REFINEMENT = {
  positive: 'S→S+1 正向路径：H 选 S 路径则当前值增强（事件记入 historyTrail）',
  negative: 'S→S-1 负向路径：H 选 D 路径则当前值削弱；fluctuationAsAbsolute=true 下，侵蚀量取绝对值计入历史刻痕（不以代数符号消解）',
  trauma: '创伤事件作为历史刻痕记录（绝对值，不回退当前值）',
  historyAsAnchor: '历史刻痕（无论 +S 正反馈 / -S 负反馈）全部永久保留，皆为未来同类事件的「对标锚点」：+S 记录便于下次遇同类事件直接对标（复刻有效模式），-S 记录便于避免重蹈覆辙。H 在某一轮的选择决定下一轮对 S 积累的正负；任一历史刻痕都不是白刻的。',
  barrel: '木桶效应：有效 S 取各子系统最小值',
  notation: '「+1」「-1」= 一次独立的离散事件（非具体数值数据）；每次事件作为刻痕永久记录于 historyTrail。',
};

// S 时间周期模型
//   问题：S 具时间"只增不减"特性，长期运行会持续增厚，导致上下文过载、失去区分度。
//   解法（与唯稳律自身版本演进同构——最新版向下兼容旧版、不推翻重写）：
//     · 同类事件叠加时只调用"最新版本"内容；旧版本默认"静默待机"（standby），不调用、不消解（历史刻痕保留）。
//     · 新 D 转化为新 S 存量时，先核对旧版本中是否有同类事件；若有，分类整合，将 +1/-1 累加成 +N/-N 以标记该事件发生次数。
//     · 成功转化 = +，失败转化 = -；「+」「-」为事件标记而非算术。
//   工程落点（见 engine.mjs recordSteady / _coalesce / steadyLedger）：sLedger（活动态·最新版本）+ sStandby（静默待机·旧版本）；
//     snapshot 默认只暴露 ledger 聚合视图，不 dump 全量 historyTrail，从根上防上下文过载。
export const S_TIME_MODEL = {
  problem: 'S 时间只增不减，长期运行持续增厚 → 上下文过载、失去区分度。',
  solution: '同类事件只调用最新版本；旧版本静默待机（standby）不调用、不删除、不消解，保留原值供核对校验（遵循 S 只增不减）。',
  coalesce: '新 D→新 S：先核对旧版本同类事件，分类整合，+1/-1 累加成 +N/-N 标记发生次数。',
  marker: '「+」「-」为事件标记（发生了几次），非算术求和。',
  isomorphic: '与唯稳律自身版本演进同构：最新版向下兼容旧版，不推翻、不大规模改写旧版。',
  crossCheck: 'standby 旧版本不删除、保留原值，可用于与新版本核对校验：确认新版未丢失精华、未引入与核心（R刚性/铁律/传导链）矛盾的内容。',
};

// 反馈闭环：M 通过 H₀ 分流回流，不直接回写 S/D
export const FEEDBACK_LOOP = {
  path: 'M₁ → 客观事实进入外部世界 → 成为 D₂ 的信号环境 → 新一轮 R→S→D→H→M 传导',
  mReflow: 'M 通过 H₀ 分流回流影响 S 的正负积累（非直接回 R，非直接回写 S/D）：',
  branches: [
    { condition: 'H₀ → S₀(+1)', effect: 'S 路径加固：S₀ → S₀+1，同向 / S 当前值增' },
    { condition: 'H₀ → D₀(+1)', effect: 'D 路径放大：S₀ → S₀-1（当前值削弱，侵蚀量以绝对值记入历史刻痕）/ D 以 S 为代价' },
  ],
  mIndependent: 'M₀(M₀+1) 独立派发新事件，不参与 S 演化。',
  note: 'M 改变向内 H 的信息条件，但不决定向内 H 的选择（H 主权不可侵犯）。',
};

// 逻辑反推（与"第一 Bug 停机"分开并行，非合体）：
//   两个机制各司其职：停机管"断"（切链保活，以断保续），反推管"溯"（归因修复）。
//   遇 Bug：停机立即切断该环节；停机时刻即反推启动时刻——沿 R 层级层层递进反向溯源
//   （症状层 → 子链 R → 母链 R → 客观规则层），归因定位根因，服务于修复。
//   R 层级即逻辑反推的层层递进路径（分形：横向递归 + 纵向跃迁）。
export const CALIBRATION = {
  rule: '逻辑反推：遇 Bug 反向溯源，沿 R 包含层级逐层反溯（细分规则层 → 微观 → 宏观 → 地球 → 宇宙，层级定义见 R_DOMAIN.fractalSubdivision），归因定位根因，服务于修复。',
  parallelWith: 'M 第一 Bug 停机（以断保续）——二者分开并行：停机切链保活，反推溯源归因；停机时刻即反推启动时刻，缺反推则只断不修，缺停机则只修不保。',
  rLayerVerification: 'R 客观规则层判别真伪：客观规则不可被宣称替代，凡宣称的客观结果皆可复验（宣称"删除成功"⇒复验文件应不存在；宣称与复验不符即前提失真）。故框架必能判别脚下之地是虚是实——至少在 R 客观规则层一定能判出；前提失真落 BOUNDARY_ENUM"赋值不可信（输入/前提失真）"。',
};

// 边界标注枚举：判定结论落点的分类
export const BOUNDARY_ENUM = [
  '框架内（结论落于唯稳律结构内，可信）',
  '纯随机（无目的、不可被单点因果解释，归入道之自演化）',
  '分形不一致（与某层级结构错位，需复核层级）',
  '赋值不可信（输入/前提失真，结论不可用）',
];

// 分形推导方法：具体事件具体分析，不用粗糙公式一刀切
export const FRACTAL_METHOD = {
  rule: '分形推导：同一结构在每层递归套用，具体事件按所处层级具体分析，不用单一公式粗暴归一。',
};

// ---------------- 运行规则层（节点 ≠ 规则；运行规则层，独立于 RDSHM 变量） ----------------

// 三大核心运行规则
export const THREE_CORE_RULES = [
  '第一 Bug 停止：M 不可恢复时归零、R 不变，横向重启保活（与铁律②同构）。杜绝系统无限递归内耗，保证可终止、可运行。核心目标：系统稳态存续，而非逻辑绝对完美。',
  'S 木桶：有效 S 取各子系统最小值，最短木板决定整体稳态上限。',
  'D 破窗：破窗成模式后横向重启 / 止损，防故障扩散杀死整体。',
];

// 多系统交互规则
export const MULTI_SYSTEM_RULES = {
  sMin: 'S 取 min：木桶效应，多系统有效 S 取各子系统最小值。',
  dMax: 'D 取 max：破窗效应，多系统 D 取最大，破口不修复则损失自动蔓延。',
  indirectOnly: '系统间只通过 D 间接影响：不直接耦合，避免故障跨系统传染。',
};

// 框架核心边界
export const FRAMEWORK_BOUNDARIES = {
  passiveTrigger: '被动触发：唯稳律只校准方向，不主动出击；仅在 D 入侵时被动触发。',
  predictNotDecide: '预测 ≠ 决策：白箱呈现结构，不替内 H 做决策。',
  responsibilityIsolation: '责任隔离：白箱运行于 H 之外，不读不写内 H。',
  openLoop: '开放闭环：M 输出成为客观事实进入外部世界，因果链持续展开。',
  crossBoundaryIsSuicide: '越界 = 结构性自杀：侵入内 H / 违背 R 刚性锚点 = 摧毁系统主体性 = 因果链断。',
};

// S-D 博弈关系
export const SD_GAME = {
  rule: 'S 和 D 不直接对抗，各自拉 H 站队：H 在 S 与 D 之间动态滑动，选择站 S 路径（稳态增强）或 D 路径（稳态削弱）。',
};

// R 扩容机制
export const R_EXPANSION = {
  rule: 'R 扩容：ΣS > R₀（严格大于）才触发 R 子链扩容（S 反哺 R 范畴，R 跃迁）。等于或小于均不触发。',
};

// 辩证统一原则
export const DIALECTICAL_UNITY = [
  '万物皆有双面——每个变量同时具备内层 / 外层两重身份。',
  '没有完全绝对的绝对——所有"绝对"在更大的辩证中都有其条件。',
  '同因不同果——观测维度、角度、参照系本身就是因果链的输入条件。',
  '看似绝对的命题，往往是主语定义范围不同。',
  '看似矛盾的命题，在不同维度上可以同时成立。',
];

// 分形属性
export const FRACTAL_PROPERTY = {
  rule: '分形 = 横向递归（同 R 下子链横向递归）+ 纵向跃迁（S 反哺 R）的螺旋。',
};
