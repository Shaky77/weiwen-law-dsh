# Treat HTML docs/demos as documentation so GitHub reports JavaScript as the
# primary language. DSH plugin radar discovery pre-checks package.json and
# clones repos; an HTML-primary repo can be misclassified and skipped.
*.html linguist-documentation
